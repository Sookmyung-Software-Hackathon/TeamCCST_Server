export interface IocContainer {
    get<T>(controller: {
        prototype: T;
    }): T;
    get<T>(controller: {
        prototype: T;
    }): Promise<T>;
}
export declare type IocContainerFactory<T = any> = (request: T) => IocContainer;
